El error aparece en: client/src/components/AddCompanyModal.tsx 

El modal se envía, pero aparece el toast rojo:
“No se pudo registrar la empresa: Error al crear la empresa”.

Antes funcionaba correctamente; ahora falla incluso cuando lleno todos los campos, incluyendo redes sociales, PDF, fotos y demás.

No estoy cambiando límites de plan: quiero que este flujo vuelva a funcionar sin importar si hay archivos o no.

Objetivo claro:
Quiero que la creación de empresas como administrador vuelva a funcionar SIEMPRE cuando se usa el formulario correctamente. No quiero volver a ver ese error genérico nunca más. Quiero que el flujo acepte todo lo que el usuario ingrese y procese el formulario sin bloquearse.

Tareas que debes realizar (sin escribir código desde cero):

Revisar el handler de envío de AddCompanyModal.tsx cerca de la línea 966 y detectar por qué la petición cae en error.

Asegurar que el payload se construya correctamente, incluyendo:

Redes sociales (aunque sean opcionales).

PDF adjunto (archivo único).

Galería de imágenes (pueden ser 0 o muchas).

Campos vacíos que deben enviarse como valores válidos, no undefined.

Revisar si el envío debe ser FormData o JSON y corregir encabezados en caso de ser necesario.

Validar que los nombres de los campos enviados coincidan EXACTAMENTE con lo que el backend espera.

Manejar errores de backend correctamente:

Si el backend devuelve un error verdadero, mostrar el mensaje real.

Si no hay error válido, nunca mostrar el mensaje genérico “Error al crear la empresa”.

Corregir cualquier validación del frontend que interrumpa el envío sin mostrar el motivo.

Asegurar que cargar PDF y fotos nunca bloquee el envío por un formato incorrecto del payload.

Evitar regresiones: no romper el flujo de edición, listado o creación de empresa por parte de otros roles.

Criterios de éxito:

Crear una empresa como administrador funciona con:

Todos los campos llenos.

PDF cargado.

Imágenes cargadas.

Redes sociales llenas o vacías.

No aparece el error genérico.

Si el backend rechaza algo real, se muestra el mensaje exacto y claro.

Los datos se guardan correctamente y el modal se cierra tras éxito.

Explicación final que necesito:
Al finalizar, explícame qué estaba mal, qué corregiste y cómo funciona ahora, sin inventar ni asumir lógicas nuevas que rompan el sistema. Solo ajusta lo necesario para que el flujo vuelva a funcionar como antes, pero más robusto y sin errores silenciosos.